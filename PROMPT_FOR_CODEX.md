$baton-substrate-builder

Build Baton Studio end-to-end.

Rules:
- Read AGENTS.md and all docs in docs/ first.
- Implement a premium UI per docs/UX_SPEC.md.
- Make it work with *no API keys* via demo simulation.
- Also implement real Claude Code integration via MCP (docs/CLAUDE_CODE_INTEGRATION.md).
- Keep going until `make dev`, `make check`, `make e2e`, and `make demo` all pass.

Deliverables:
- Working backend + frontend + mcp server
- Docs updated and accurate
- Screenshots in assets/ui produced by Playwright or a scripted process
